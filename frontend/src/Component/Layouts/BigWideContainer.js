import React from "react";
import { Container, Image } from "semantic-ui-react";
const BigWideContainer = items => (
  <Container textAlign="center" fluid>
    {items}
  </Container>
);
export default BigWideContainer;
