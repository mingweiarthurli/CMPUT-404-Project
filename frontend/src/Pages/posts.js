import React, { Fragment } from "react";
import { Container } from "semantic-ui-react";

const posts = () => (
  <Fragment>
    <Container>Browse Posts</Container>
  </Fragment>
);

export default posts;
